import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ["i0.wp.com", "accountingprofessional4.wpcomstaging.com"],
    },
    // add any additional config options here
};

export default nextConfig;
